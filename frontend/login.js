const form=document.querySelector('#loginForm'),username=document.querySelector('#username'),password=document.querySelector('#password'),error=document.querySelector('#loginError'),signIn=document.querySelector('#signIn');
document.querySelectorAll('.demo-accounts button').forEach(button=>button.onclick=()=>{username.value=button.dataset.user;password.value=button.dataset.password;error.textContent='';password.focus();});
form.onsubmit=async event=>{event.preventDefault();error.textContent='';signIn.disabled=true;signIn.textContent='Signing in…';try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:username.value.trim(),password:password.value})});const data=await response.json();if(!response.ok)throw Error(data.error||'Sign-in failed');location.replace('/app.html');}catch(reason){error.textContent=reason.message;}finally{signIn.disabled=false;signIn.textContent='Sign in';}};
fetch('/api/auth/me',{headers:{'Accept':'application/json'}}).then(response=>{if(response.ok)location.replace('/app.html');}).catch(()=>{});
